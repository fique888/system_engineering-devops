#!/usr/bin/env bash
#
for file in *; do
  # Skip hidden files and non-regular files (like directories, symlinks, etc.)
  [[ "$file" == .* ]] && continue
  [ -f "$file" ] || continue

  # Check if file name contains a dash
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done

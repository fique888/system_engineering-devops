#!/bin/bash

for file in *; do
  # skip if it is a hidden file (already filtered by *), but double check if needed:
  [[ "$file" == .* ]] && continue
  
  # check if file contains a dash
  if [[ "$file" == *-* ]]; then
    # extract part after first dash
    echo "${file#*-}"
  fi
done
